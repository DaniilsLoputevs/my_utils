package sounding;

/**
 * Короче:
 * 1 - показывать инфу о классах Родителях и Интерфэйсах(полный путь до класса + каждый класс на новой строке).
 * 2 - инфа о полях(модификаторы в цвете). Объекты разварачивать просто toSting не катит.
 * 3 - (Опционально) инфа о методах(для корня и для вложенных объектов)
 * @version 0.1 - 20.07.2021
 */
public interface Documentation {
}
