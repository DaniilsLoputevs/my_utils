package mappings

/**
 * TODO : запрос c 0 аргументов - Не проблема
 * TODO : запрос c 1 аргументов - Не проблема
 * TODO : запрос c 2 аргументов - Не проблема
 * TODO : запрос c 3 аргументов - Не проблема
 * TODO : security - Не проблема - pathCtx
 * TODO : permission - Не проблема - pathCtx
 * TODO : current user - Не проблема - pathCtx
 * TODO : activityId - Не проблема - pathCtx
 * TODO : primitives - Не проблема - ValueWrapper<T>
 * TODO :
 */
fun userControllerPaths() {














    path<UserK>("testUser") {
        print("it work! $it")
    }

    path<UserK>("testUser") {
        print("it work! $it")
    }

    path<UserK>("testUser") {
        print("it work! $it")
    }

    path<UserK>("testUser") {
        print("it work! $it")
    }

    path<UserK>("testUser") {
        print("it work! $it")
    }

    path<UserK>("testUser") {
        print("it work! $it")
    }

    path<UserK>("testUser") {
        print("it work! $it")
    }

    path<UserK>("testUser") {
        print("it work! $it")
    }
}